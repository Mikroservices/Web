import { EmailConnectedValidatorDirective } from './email-connected-validator.directive';

describe('EmailConnectedValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailConnectedValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
